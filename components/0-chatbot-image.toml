# docker-build
name = "chatbot_image"
type = "docker_build"

dockerfile    = "Dockerfile"
build_timeout = "15m"

[public_repo]
repo      = "matt-zach-s/byoc-agent-starter"
directory = "src/app"
branch    = "main"
